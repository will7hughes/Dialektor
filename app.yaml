runtime: python38

 # for Google Cloud Flexible App Engine

# any environment variables you want to pass to your application.
# accessible through os.environ['VARIABLE_NAME']
env_variables:
# the secret key used for the Django app (from PROJECT-DIRECTORY/settings.py)
  SECRET_KEY: '7b7^dshdx=*&zval+53)z7vzzttfmvs=r2y@ec0ar-0oaw=ug_'
  DEBUG: 'False' # always False for deployment

# everything after /cloudsql/ can be found by entering >> gcloud sql instances describe DATABASE-NAME << in your Terminal
# the DATABASE-NAME is the name you gave your project's PostgreSQL database
# the second line from the describe output called connectionName can be copied and pasted after /cloudsql/
  DB_HOST: '/cloudsql/djangokubernetes-303405:us-central1:cloud-sql-postgresql'
  DB_PORT: '5432' # PostgreSQL port
  DB_NAME: 'dialektorproductiondb'
  DB_USER: 'dialektoruser' # either 'postgres' (default) or one you created on the PostgreSQL instance page
  DB_PASSWORD: 'M>t)X:F+t4Hs9eja'

  STATIC_URL: 'https://storage.googleapis.com/dialektor-static-bucket/static/' # this is the url that you sync static files to


handlers:
- url: /static
  static_dir: static
- url: /.*
  script: auto

beta_settings:
# from command >> gcloud sql instances describe dialektor-db <<
  cloud_sql_instances: djangokubernetes-303405:us-central1:cloud-sql-postgresql

runtime_config:
  python_version: 3 # enter your Python version BASE ONLY here. Enter 2 for 2.7.9 or 3 for 3.6.4
